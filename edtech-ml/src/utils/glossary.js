/**
 * Общий глоссарий терминов платформы.
 * Эти термины автоматически подсвечиваются в тексте теории.
 * Поддерживает Markdown (включая LaTeX через $...$ и блоки кода).
 */
export const GLOBAL_GLOSSARY = {
  // --- Метрики и ошибки ---
  'MSE': {
    definition: `**Среднеквадратичная ошибка (Mean Squared Error)** — это квадратичная функция потерь, измеряющая среднее значение квадратов отклонений предсказанных значений модели от истинных ответов. Математически определяется как:

$$MSE = \\frac{1}{n} \\sum_{i=1}^{n} (y_i - \\hat{y}_i)^2$$

Где $y_i$ — истинное значение, $\\hat{y}_i$ — предсказание, $n$ — количество образцов. Возведение в квадрат гарантирует положительность ошибки и накладывает прогрессивный «штраф» за большие отклонения.`,
    example: `| Отклонение ($y - \\hat{y}$) | Ошибка ($MSE$ компонент) |
| :--- | :--- |
| 0.5 | 0.25 |
| 1.0 | 1.00 |
| 2.0 | 4.00 |
| 10.0 | 100.00 |

**Аналогия:** Представьте настройку фокуса микроскопа. Если вы немного промахнулись, картинка чуть размыта. Но если промах велик — вы не видите ничего. MSE заставляет модель фокусироваться в первую очередь на устранении самых больших «размытий» (ошибок).`
  },
  'RMSE': {
    definition: `**Root Mean Squared Error** — это квадратный корень из MSE. Она возвращает метрику в размерность исходной целевой переменной, что делает её более интерпретируемой для анализа.

$$RMSE = \\sqrt{\\frac{1}{n} \\sum_{i=1}^{n} (y_i - \\hat{y}_i)^2}$$`,
    example: `Если мы обучаем модель предсказывать стоимость недвижимости в долларах ($):
- **MSE** выдаст результат в «квадратных долларах» ($\\$^2$), что лишено физического смысла.
- **RMSE** выдаст результат в обычных долларах ($\\$$).

Если $RMSE = 5000$, это буквально означает, что в среднем модель ошибается на 5000 долларов в большую или меньшую сторону.`
  },
  'MAE': {
    definition: `**Средняя абсолютная ошибка (Mean Absolute Error)** — метрика, вычисляемая как среднее арифметическое модулей разностей между истинными и предсказанными значениями.

$$MAE = \\frac{1}{n} \\sum_{i=1}^{n} |y_i - \\hat{y}_i|$$

В отличие от MSE, MAE устойчива (robust) к выбросам, так как штрафует за ошибку линейно, а не квадратично.`,
    example: `Рассмотрим датасет с одним аномальным значением (выбросом):
- Ошибки на обычных точках: $[1, 2, 1, 3]$
- Ошибка на выбросе: $100$

**Сравнение влияния:**
- **MAE:** $(1+2+1+3+100)/5 = 21.4$
- **MSE:** $(1^2+2^2+1^2+3^2+100^2)/5 = 2003$

MSE «сходит с ума» от одной точки, в то время как MAE сохраняет более адекватный масштаб.`
  },
  'R2': {
    definition: `**Коэффициент детерминации ($R^2$)** — статистическая мера, определяющая долю дисперсии зависимой переменной, которая объясняется моделью.

$$R^2 = 1 - \\frac{SS_{res}}{SS_{tot}} = 1 - \\frac{\\sum (y_i - \\hat{y}_i)^2}{\\sum (y_i - \\bar{y})^2}$$

Где $SS_{res}$ — сумма квадратов остатков, $SS_{tot}$ — общая сумма квадратов (относительно среднего $\\bar{y}$).`,
    example: `Интерпретация значений:
- $R^2 = 1.0$: Идеальное предсказание.
- $R^2 = 0.0$: Модель работает не лучше, чем простое предсказание среднего значения всех ответов.
- $R^2 < 0.0$: Модель работает хуже, чем предсказание среднего (ужасный результат).

**Аналогия:** Если вы пытаетесь предсказать рост человека, зная его возраст. $R^2$ покажет, насколько возраст помогает объяснить вариацию роста в популяции.`
  },

  // --- Основные понятия ---
  'Градиентный спуск': {
    definition: `**Градиентный спуск (Gradient Descent)** — итерационный алгоритм первого порядка для поиска локального минимума дифференцируемой функции. В ML он используется для минимизации функции потерь $L(w)$ путем обновления весов $w$ в направлении, противоположном вектору градиента $\\nabla L(w)$.

$$w_{new} = w_{old} - \\eta \\cdot \\nabla L(w_{old})$$`,
    example: `**Алгоритм спуска:**
1. Выбрать случайную точку старта (инициализация весов).
2. Вычислить градиент (куда «растет» функция).
3. Сделать шаг $\\eta$ (Learning Rate) в обратную сторону.
4. Повторять до сходимости.

**Аналогия:** Спуск с горы в густом тумане. Вы не видите вершину или подножие, но чувствуете наклон поверхности стопами. Вы делаете осторожный шаг туда, где чувствуете спуск.`
  },
  'Learning Rate': {
    definition: `**Скорость обучения ($\\eta$ или $\\alpha$)** — гиперпараметр, определяющий величину шага на каждой итерации градиентного спуска. Он контролирует скорость коррекции весов в ответ на оцененную ошибку.`,
    example: `| Стратегия | Последствие |
| :--- | :--- |
| **Слишком высокий** | Осцилляция (скачки вокруг минимума) или расходимость (улет в бесконечность). |
| **Слишком низкий** | Чрезвычайно долгое обучение, риск застревания в плохих локальных минимумах. |
| **Оптимальный** | Плавное и уверенное снижение ошибки до минимума. |

В современном ML часто используют *Learning Rate Schedulers*, которые уменьшают шаг по мере приближения к цели.`
  },
  'Веса': {
    definition: `**Веса (Weights, $w$)** — это настраиваемые параметры модели, которые определяют важность (силу влияния) каждого входного признака на итоговый результат. В линейных моделях веса являются коэффициентами перед признаками.`,
    example: `В задаче оценки кредитного риска:
- Признак «Доход»: вес $w_1 = 0.8$ (очень важен).
- Признак «Цвет глаз»: вес $w_2 = 0.0001$ (не важен).
- Признак «История долгов»: вес $w_3 = -1.5$ (отрицательное влияние — долги снижают рейтинг).`
  },
  'Смещение': {
    definition: `**Смещение (Bias, $b$)** — свободный член в уравнении модели, который позволяет разделяющей поверхности или регрессионной прямой сдвигаться относительно начала координат.

В линейной модели: $y = w \cdot x + b$.`,
    example: `Представьте, что вы предсказываете цену дома. Даже если площадь дома ($x$) равна 0, земля под ним всё равно стоит денег. Эти базовые затраты и отражает **Смещение**. 

Без $b$ любая прямая обязана была бы проходить через точку $(0,0)$, что сильно ограничивает гибкость модели.`
  },

  // --- Проблемы обучения ---
  'Переобучение': {
    definition: `**Переобучение (Overfitting)** — явление, при котором модель чрезмерно подстраивается под специфические особенности (шум) обучающей выборки, теряя при этом способность к обобщению (generalization) на новых данных. Сопровождается низкой ошибкой на обучении и высокой — на тесте.`,
    example: `**Причины:**
- Слишком сложная модель (слишком много параметров).
- Слишком мало данных.
- Обучение в течение слишком большого количества эпох.

**Аналогия:** Студент, который вместо понимания законов физики просто зазубрил все ответы из задачника прошлого года. Если на экзамене попадется та же задача — он ответит идеально. Если условие чуть изменится — он не сможет решить ничего.`
  },
  'Недообучение': {
    definition: `**Недообучение (Underfitting)** — состояние, когда модель не может выявить скрытую структуру данных из-за недостаточной сложности или неадекватного процесса обучения. Ошибка высока как на обучающей, так и на тестовой выборках.`,
    example: `**Причины:**
- Модель слишком проста (например, попытка описать параболу прямой линией).
- Плохой выбор признаков.
- Слишком агрессивная регуляризация.

**Аналогия:** Попытка объяснить теорию относительности пятилетнему ребенку, используя только слова «мама», «папа» и «каша». Инструментарий слишком беден для сложности задачи.`
  },
  'Регуляризация': {
    definition: `**Регуляризация** — методы внесения дополнительной информации или ограничений в процесс обучения для предотвращения переобучения. Обычно реализуется через добавление «штрафного» слагаемого $R(w)$ к основной функции потерь $J(w)$.

$$J_{reg}(w) = J(w) + \\lambda R(w)$$`,
    example: `Коэффициент $\\lambda$ (лямбда) управляет балансом:
- При $\\lambda \\to 0$: штрафа нет (риск переобучения).
- При $\\lambda \\to \\infty$: модель стремится сделать все веса нулевыми (недообучение).

Это способ сказать модели: «Будь точной, но сохраняй свои коэффициенты как можно более простыми и маленькими».`
  },

  // --- Данные и признаки ---
  'Выброс': {
    definition: `**Выброс (Outlier)** — это точка данных, которая существенно отклоняется от общей закономерности распределения в выборке. Выбросы могут быть как следствием ошибок измерения, так и редкими реальными событиями.`,
    example: `Если в классе у 20 детей рост от 140 до 160 см, а у одного — 210 см, этот ребенок будет **Выбросом**. 

Алгоритмы вроде **MSE** будут стараться "дотянуться" до этой точки, что может сильно исказить финальную модель (например, наклон прямой регрессии).`
  },
  'Нормализация': {
    definition: `**Нормализация (Min-Max Scaling)** — преобразование признаков в фиксированный диапазон, обычно $[0, 1]$. Это критично для алгоритмов, использующих дистанционные метрики (k-NN, SVM) или градиентные методы.

$$x' = \\frac{x - \min(x)}{\max(x) - \min(x)}$$`,
    example: `Представьте данные о людях:
- Вес в кг ($40...150$).
- Рост в метрах ($1.4...2.1$).

Без нормализации изменение веса на 1 кг будет казаться алгоритму в 1000 раз более значимым, чем изменение роста на 1 см, хотя это не так. Нормализация делает их сопоставимыми.`
  },
  'Стандартизация': {
    definition: `**Стандартизация (Z-score normalization)** — центрирование данных относительно нуля с приведением к единичному стандартному отклонению.

$$z = \\frac{x - \\mu}{\\sigma}$$

Где $\\mu$ — среднее значение, $\\sigma$ — стандартное отклонение. Большинство алгоритмов (SVM, Линейная регрессия) предполагают, что данные распределены именно так.`,
    example: `После стандартизации:
- Значение $z = 0$ означает, что объект находится точно на «среднем» уровне.
- $z = 2$ означает, что объект на два стандартных отклонения «выше» среднего.
- Почти все данные (99.7%) будут лежать в диапазоне от $-3$ до $3$.`
  },
  'Feature Engineering': {
    definition: `**Конструирование признаков (Feature Engineering)** — процесс создания новых входных параметров модели на основе существующих данных для повышения её предсказательной способности.`,
    example: `Если у вас есть координаты (широта и долгота), вы можете создать признак «Расстояние до центра города». Этот один признак часто важнее для предсказания цены квартиры, чем две сырые координаты по отдельности.`
  },
  'DataFrame': {
    definition: `**DataFrame** — основная структура данных библиотеки **Pandas**, представляющая собой двумерную таблицу с именованными строками и столбцами. Это аналог таблицы SQL или листа Excel в Python.`,
    example: `Объект DataFrame позволяет выполнять сложные операции одной строкой:
\`\`\`python
# Фильтрация и вычисление среднего
df[df['age'] > 30]['salary'].mean()
\`\`\`
Каждый столбец в DataFrame — это объект Series (одномерный массив).`
  },

  // --- Выборки ---
  'Обучающая выборка': {
    definition: `**Обучающая выборка (Training Set)** — часть данных, которая используется непосредственно для настройки параметров модели (подбора весов). Модель "видит" правильные ответы для этих данных и минимизирует функцию потерь на них.`,
    example: `Это как задачи, которые студент решает в классе вместе с учителем. Он учится находить закономерности, опираясь на подсказки и разбор ошибок.`
  },
  'Тестовая выборка': {
    definition: `**Тестовая выборка (Test Set)** — часть данных, скрытая от модели в процессе обучения. Используется для финальной оценки качества и проверки способности модели обобщать знания на новые, ранее не виденные примеры.`,
    example: `Это экзаменационный билет. Студент не видел его заранее. Если он хорошо решал задачи в классе, но провалил экзамен — значит, произошло **Переобучение** (зубрежка).`
  },

  // --- Алгоритмы ---
  'Регрессия': {
    definition: `**Регрессия** — тип задачи машинного обучения, целью которой является предсказание непрерывной числовой величины (в отличие от классификации, где мы предсказываем категорию).`,
    example: `**Примеры задач регрессии:**
- Предсказание температуры воздуха на завтра.
- Оценка рыночной стоимости автомобиля.
- Прогноз количества проданных товаров в следующем месяце.`
  },
  'Линейная регрессия': {
    definition: `**Линейная регрессия** — метод прогнозирования целевой переменной $y$ как линейной комбинации входных признаков $x_i$ с добавлением свободного члена.

$$\\hat{y} = w_0 + w_1x_1 + w_2x_2 + ... + w_nx_n$$

Обучение заключается в поиске таких весов $w$, которые минимизируют MSE.`,
    example: `**Простая линейная регрессия ($y = wx + b$):**
- $x$: Площадь квартиры.
- $w$: Стоимость одного квадратного метра.
- $b$: Базовая стоимость подключения коммуникаций.
- $y$: Итоговая цена.

Визуально это поиск «лучшей прямой», которая проходит максимально близко ко всем точкам данных на графике.`
  },
  'Логистическая регрессия': {
    definition: `**Логистическая регрессия** — алгоритм классификации (несмотря на название), используемый для оценки вероятности принадлежности объекта к классу. Результат линейной комбинации признаков пропускается через сигмоидную функцию.

$$P(y=1|x) = \\sigma(w^T x) = \\frac{1}{1 + e^{-w^Tx}}$$`,
    example: `**Выход модели:**
- Результат $0.85$: Вероятность того, что письмо — спам, равна 85%.
- Если мы установим порог (threshold) $0.5$, то письмо будет помечено как «Спам».

Сигмоида «сплющивает» любую бесконечную прямую в диапазон от $0$ до $1$, превращая числа в вероятности.`
  },
  'SVM': {
    definition: `**Метод опорных векторов (Support Vector Machine)** — мощный алгоритм классификации, который строит разделяющую гиперплоскость таким образом, чтобы максимизировать зазор (margin) между ближайшими точками разных классов.`,
    example: `Опорные вектора — это те самые "критические" точки данных, которые лежат ближе всего к границе. Если их немного сдвинуть, изменится и сама граница. Остальные точки на положение границы не влияют.`
  },
  'Перцептрон': {
    definition: `**Перцептрон** — элементарная единица нейронной сети (искусственный нейрон), предложенная Фрэнком Розенблаттом. Он суммирует входные сигналы с весами, добавляет смещение и выдает результат через пороговую функцию активации.`,
    example: `| Компонент | Роль |
| :--- | :--- |
| **Веса** | Важность каждого входящего сигнала. |
| **Смещение** | Базовый порог активации. |
| **Функция активации** | Решает, "выстрелит" нейрон (1) или промолчит (0). |

Это математическая модель того, как работает биологический нейрон в мозгу человека.`
  },

  // --- Функции активации ---
  'Сигмоида': {
    definition: `**Сигмоида (Логистическая функция)** — гладкая монотонная функция, которая отображает любое вещественное число в интервал $(0, 1)$. Математически выражается как:

$$\\sigma(z) = \\frac{1}{1 + e^{-z}}$$`,
    example: `Она превращает "очки" или "логиты" модели в вероятности. 
- Если $z=0$, $\\sigma(z) = 0.5$.
- Если $z \\to \\infty$, $\\sigma(z) \\to 1$.
- Если $z \\to -\\infty$, $\\sigma(z) \\to 0$.

Используется в **Логистическая регрессия** и в выходных слоях нейронных сетей для бинарной классификации.`
  },

  // --- Классификация ---
  'Precision': {
    definition: `**Точность (Precision)** — метрика, показывающая долю объектов, действительно являющихся положительными, среди всех объектов, которые модель классифицировала как положительные.

$$Precision = \\frac{TP}{TP + FP}$$`,
    example: `**Ситуация:** Система безопасности банка отмечает подозрительные транзакции.
- Если из 10 заблокированных переводов 9 были реально мошенническими — **Precision высокая (0.9)**.
- Если из 10 заблокированных 9 были честными — **Precision низкая (0.1)**. Банк зря беспокоит клиентов.`
  },
  'Recall': {
    definition: `**Полнота (Recall / Sensitivity)** — метрика, показывающая, какую долю реальных положительных объектов из всей выборки удалось обнаружить модели.

$$Recall = \\frac{TP}{TP + FN}$$`,
    example: `**Ситуация:** Медицинский тест на редкое заболевание.
- Если в городе 100 больных, а тест нашел только 20 — **Recall низкая (0.2)**. 80 человек уйдут домой, не зная о болезни.
- Для медицины Recall часто важнее, чем Precision: лучше перепроверить здорового, чем пропустить больного.`
  },
  'F1-score': {
    definition: `**F1-мера** — сбалансированная метрика, являющаяся гармоническим средним между Precision и Recall. Она «наказывает» за сильный перекос в сторону одной из метрик.

$$F1 = 2 \\cdot \\frac{Precision \\cdot Recall}{Precision + Recall}$$`,
    example: `Почему не обычное среднее?
- Модель А: $P=0.9, R=0.1$ (Бесполезная модель). Обычное среднее: $0.5$. **F1: $0.18$** (Справедливо низко).
- Модель Б: $P=0.5, R=0.5$. Обычное среднее: $0.5$. **F1: $0.5$**.

F1-score полезен, когда у вас есть дисбаланс классов (например, 1% мошенников и 99% честных операций).`
  },
  'Матрица ошибок': {
    definition: `**Confusion Matrix** — это инструмент оценки качества классификации в виде таблицы, которая позволяет визуализировать ошибки модели, разделяя их на типы: ложноположительные и ложноотрицательные.`,
    example: `| | Предсказано: Negative | Предсказано: Positive |
| :--- | :--- | :--- |
| **Факт: Negative** | **TN** (Верно отвергли) | **FP** (Ошибка I рода) |
| **Факт: Positive** | **FN** (Ошибка II рода) | **TP** (Верно нашли) |

**Пример:**
- **FP (Ложная тревога):** Тест сказал «беременна», а это мужчина.
- **FN (Пропуск цели):** Тест сказал «не беременна», а женщина рожает.`
  },

  // --- Инструменты ---
  'NumPy': {
    definition: `**NumPy** — фундаментальная библиотека для научных вычислений в Python. Поддерживает работу с многомерными массивами (ndarrays) и предоставляет огромный набор высокоуровневых математических функций.`,
    example: `Главное преимущество — **векторизация**. Вычисления с массивами NumPy выполняются в десятки раз быстрее, чем циклы в чистом Python, так как основной код написан на C и оптимизирован.`
  },
  'Дисперсия': {
    definition: `**Дисперсия (Variance)** — мера разброса предсказаний модели при обучении на разных выборках данных. Высокая дисперсия указывает на то, что модель слишком чувствительна к случайным колебаниям в обучающем наборе (**Переобучение**).`,
    example: `Представьте стрелка, который попадает в разные части мишени (большой разброс), хотя в среднем он целится в центр. Это и есть высокая дисперсия.`
  },
  'Сложность модели': {
    definition: `**Сложность модели** — характеристика, определяющая способность модели описывать сложные зависимости. Обычно зависит от количества свободных параметров (например, количество весов в нейронной сети или степень полинома в регрессии).`,
    example: `Прямая линия — это модель низкой сложности. Извилистая кривая, проходящая через каждую точку — модель чрезмерно высокой сложности.`
  },
  'Логистические потери': {
    definition: `**Логистические потери (Log Loss / Cross-Entropy)** — функция потерь, используемая в задачах классификации для оценки качества предсказания вероятностей.

$$L(y, p) = -(y \log(p) + (1-y) \log(1-p))$$

Где $y$ — истинная метка ($0$ или $1$), $p$ — предсказанная вероятность.`,
    example: `Если реальный класс $1$, а модель предсказала вероятность $0.01$, штраф будет огромным (стремится к бесконечности), так как модель "уверена" в неверном ответе.`
  },
  'Pandas': {
    definition: `**Pandas** — высокоуровневая библиотека для анализа и манипуляции данными. Построена поверх **NumPy**. Предоставляет удобные структуры данных для работы с таблицами и временными рядами.`,
    example: `С помощью Pandas можно за пару строк кода загрузить CSV-файл, заполнить пропущенные значения, сгруппировать данные по категориям и вычислить статистику.`
  }
};
