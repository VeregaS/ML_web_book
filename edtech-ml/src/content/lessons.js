export const lessons = [
  {
    id: "ml-math-tensors-1-expanded",
    module: "Математические основы ML",
    title: "Урок 1: От скаляров к тензорам",
    difficulty: "Легко",
    description: "Фундаментальное погружение в структуры данных машинного обучения: скаляры, векторы, матрицы, тензоры высших порядков, транслирование (broadcasting) и векторизация.",
    steps: [
      {
        id: "step-1-1",
        type: "jupyter",
        theory: "В машинном обучении и анализе данных все начинается с чисел, поскольку алгоритмы не понимают мир в виде образов, звуков или текстов напрямую. Фундаментальным и самым простым математическим понятием здесь является **скаляр**.\n\n**Скаляр** — это одиночное числовое значение, которое выражает величину определенного свойства объекта без указания направления. В контексте компьютерных структур данных скаляр можно определить как тензор нулевого ранга (то есть массив, у которого ровно ноль осей или измерений). \n\nВ реальных задачах машинного обучения скаляром может быть отдельное и изолированное измерение: например, температура на улице в градусах Цельсия, рост конкретного человека, цена дома или значение интенсивности одного пикселя на черно-белой фотографии. При написании кода на языке Python скаляры обычно представлены базовыми типами данных, такими как целые числа (`int`) или числа с плавающей точкой (`float`). Хотя скаляры кажутся слишком примитивными, именно из них, как из кирпичиков, строятся все остальные, гораздо более сложные математические объекты, с которыми работают современные нейронные сети и классические алгоритмы.",
        task: "Импортируйте библиотеку `numpy` как `np`. Создайте скаляр `scalar_val` со значением `5.0` в виде числа с плавающей точкой.",
        hint: "Используйте обычное присваивание в Python: `scalar_val = 5.0`.",
        initialCode: "import numpy as np\n\n# Создайте скаляр\nscalar_val = \n",
        testCode: "import unittest\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_scalars(self):\n        self.assertEqual(student_code.scalar_val, 5.0, msg=\"Скаляр создан неверно. Убедитесь, что значение равно 5.0\")"
      },
      {
        id: "step-1-2",
        type: "jupyter",
        theory: "Данные в реальном мире крайне редко приходят в виде одиночных чисел. Чаще всего мы описываем любой физический или виртуальный объект целым набором различных характеристик. Для хранения и математической обработки таких наборов используется структура, называемая **вектором**.\n\n**Вектор** — это упорядоченный одномерный массив скаляров. В математике и программировании вектор является тензором первого ранга, так как для доступа к любому его внутреннему элементу требуется только один индекс (то есть у него всего одна координатная ось). Количество элементов, содержащихся в векторе, называется его **размерностью** (dimensionality).\n\nВ машинном обучении вектор традиционно используется для описания одного независимого объекта обучающей выборки. Каждое число (или координата) в этом векторе называется **признаком** (feature) и отражает ровно одно конкретное свойство объекта. Например, вектор, описывающий автомобиль, может состоять из таких числовых признаков: `[мощность_двигателя, количество_дверей, пробег]`. Геометрически вектор можно визуализировать как направленный отрезок, исходящий из начала координат в многомерном пространстве, где каждая ось соответствует отдельному признаку.",
        task: "Используя библиотеку NumPy, создайте одномерный вектор признаков `vector_val`, состоящий из трех элементов: `10.0`, `20.0` и `30.0`.",
        hint: "Для создания вектора в NumPy используйте функцию `np.array()`, передав ей обычный список Python.",
        initialCode: "import numpy as np\n\n# Создайте одномерный вектор\nvector_val = \n",
        testCode: "import unittest\nimport numpy as np\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_vectors(self):\n        expected = np.array([10.0, 20.0, 30.0])\n        np.testing.assert_allclose(student_code.vector_val, expected, err_msg=\"Вектор создан неверно.\")"
      },
      {
        id: "step-1-3",
        type: "jupyter",
        theory: "Когда мы собираем и объединяем информацию сразу о множестве объектов (например, о тысячах пациентов клиники), нам требуется структура с двумя осями. Такой математической структурой является **матрица**.\n\n**Матрица** — это прямоугольная двумерная таблица чисел, которая состоит из строк и столбцов. Она представляет собой тензор второго ранга, поскольку для точной адресации любого элемента в ней нужны ровно два индекса: номер строки и номер столбца.\n\nВ машинном обучении каноническим и самым популярным способом представления данных является матрица «объекты-признаки» (design matrix), которая обычно обозначается заглавной латинской буквой $X$. В этой матрице каждая строка строго соответствует одному независимому объекту (наблюдению), а каждый столбец описывает значения одного конкретного признака для всех объектов сразу. Если в нашей обучающей выборке $m$ объектов, и каждый из них описан $n$ признаками, мы получим матрицу размерностью $m \\times n$. Важнейшей алгебраической операцией над матрицами является **транспонирование** ($X^T$) — это процесс зеркального отражения матрицы относительно ее главной диагонали, при котором строки матрицы превращаются в ее столбцы, а столбцы становятся строками.",
        task: "Создайте матрицу $X$ (набор данных из 2 объектов и 3 признаков) со значениями `[[1, 7, 8], [9-11]]`. Вычислите ее транспонированную версию и сохраните в переменную `X_T`.",
        hint: "Транспонировать матрицу в NumPy можно с помощью обращения к атрибуту `.T` (например, `X.T`).",
        initialCode: "import numpy as np\n\n# Создайте матрицу и транспонируйте ее\nX = \nX_T = \n",
        testCode: "import unittest\nimport numpy as np\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_matrices(self):\n        expected_X = np.array([[1, 7, 8], [9-11]])\n        np.testing.assert_array_equal(student_code.X, expected_X, err_msg=\"Матрица X создана неверно.\")\n        np.testing.assert_array_equal(student_code.X_T, expected_X.T, err_msg=\"Матрица X_T должна быть транспонированной версией X.\")"
      },
      {
        id: "step-1-4",
        type: "jupyter",
        theory: "Скаляры, векторы и матрицы — это лишь частные случаи более абстрактной, гибкой и универсальной математической концепции, которая называется **тензором**.\n\n**Тензор** — это многомерный контейнер числовых данных (многомерный массив). Количество измерений (или осей координат) тензора называется его **рангом** (или порядком). Исходя из этого определения, скаляр — это тензор ранга 0, вектор — тензор ранга 1, а матрица — тензор ранга 2.\n\nТензоры высших рангов (начиная от 3-го и выше) становятся критически важными при переходе к глубокому обучению (Deep Learning). Самый яркий практический пример — цифровые изображения. Любое цветное изображение традиционно представляется компьютером как тензор 3-го ранга, где первые две пространственные оси отвечают за высоту и ширину картинки в пикселях, а третья ось содержит независимые цветовые каналы (обычно это Red, Green, Blue или RGB-канал). Когда мы обучаем глубокую нейронную сеть, мы крайне редко подаем ей картинки по одной; вместо этого алгоритм группирует их в **пакеты** (batches). Пакет из нескольких цветных изображений образует тензор 4-го ранга с размерностями: `(размер_пакета, высота, ширина, количество_каналов)`.",
        task: "Создайте тензор 3-го ранга `image_tensor`, заполненный нулями, который имитирует одно цветное RGB изображение размером 28x28 пикселей. Его форма (shape) должна быть `(28, 28, 3)`.",
        hint: "Используйте встроенную функцию `np.zeros(shape)`, передав ей кортеж `(28, 28, 3)`.",
        initialCode: "import numpy as np\n\n# Создайте 3D тензор из нулей\nimage_tensor = \n",
        testCode: "import unittest\nimport numpy as np\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_tensors(self):\n        self.assertEqual(student_code.image_tensor.shape, (28, 28, 3), msg=\"Форма тензора должна быть (28, 28, 3).\")\n        self.assertTrue(np.all(student_code.image_tensor == 0), msg=\"Тензор должен состоять из нулей.\")"
      },
      {
        id: "step-1-5",
        type: "jupyter",
        theory: "При работе с многомерными тензорами в программировании важно строго различать виды выполняемых арифметических действий. В классической линейной алгебре умножение матриц вычисляется по сложному правилу «строка на столбец». Однако современные фреймворки машинного обучения активно опираются на **поэлементные операции** (element-wise operations).\n\n**Поэлементная операция** — это простейшее математическое действие (сложение, вычитание, умножение или деление), которое применяется абсолютно независимо к каждой паре чисел, находящихся на строго одинаковых позициях в двух тензорах одинаковой формы.\n\nПоэлементное умножение двух тензоров имеет специальное математическое название — **произведение Адамара** (Hadamard product). Эта операция невероятно востребована внутри современных нейронных сетей. Например, именно с помощью произведения Адамара работают механизмы фильтрации (пропускания и блокировки) информации в ячейках рекуррентных сетей типа LSTM. Также поэлементно применяются любые нелинейные функции активации (такие как ReLU или сигмоида) к матрицам выходов скрытых слоев нейросети.",
        task: "Даны две матрицы `A` и `B` одинакового размера. Вычислите их поэлементную сумму в переменную `M_sum` и их произведение Адамара (поэлементное умножение) в `M_hadamard`.",
        hint: "В библиотеке NumPy базовые операторы `+` и `*` автоматически выполняют поэлементные операции, если формы массивов совпадают.",
        initialCode: "import numpy as np\n\nA = np.array([[7, 8], [1, 9]])\nB = np.array([[10, 11], [12, 13]])\n\n# Выполните вычисления\nM_sum = \nM_hadamard = \n",
        testCode: "import unittest\nimport numpy as np\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_elementwise(self):\n        np.testing.assert_array_equal(student_code.M_sum, student_code.A + student_code.B, err_msg=\"Сумма вычислена неверно.\")\n        np.testing.assert_array_equal(student_code.M_hadamard, student_code.A * student_code.B, err_msg=\"Произведение Адамара вычислено неверно.\")"
      },
      {
        id: "step-1-6",
        type: "jupyter",
        theory: "Часто в реальных вычислениях возникает необходимость сложить или перемножить тензоры, чьи формы (размерности) изначально не совпадают. В строгой математике такие операции считаются неопределенными, но в библиотеках машинного обучения применяется элегантное технологическое решение — механизм **транслирования** (broadcasting).\n\n**Транслирование** — это встроенный в фреймворки (NumPy, TensorFlow) набор интеллектуальных правил, который позволяет выполнять поэлементные операции над массивами разной формы. Алгоритм виртуально «растягивает» или дублирует меньший тензор вдоль недостающих осей до тех пор, пока формы обоих тензоров не совпадут и операция не станет возможной.\n\nГлавное техническое преимущество механизма транслирования заключается в том, что физического копирования данных в оперативной памяти компьютера не происходит. Вычисления выполняются «на лету» на уровне оптимизированного кода на C, что экономит гигантские объемы памяти. Классический пример из глубокого обучения: когда к двумерной матрице выходов целого слоя нейронной сети прибавляется одномерный вектор смещений (bias), этот вектор автоматически транслируется (дублируется) на каждую строку матрицы.",
        task: "Дана матрица признаков `X` размера `(3, 2)` и вектор смещений `bias` размера `(2,)`. Прибавьте вектор `bias` к матрице `X` с помощью обычного оператора сложения `+`, чтобы произошло транслирование. Сохраните результат в `Z`.",
        hint: "Просто напишите `Z = X + bias`. Механизм транслирования сделает всю работу за вас.",
        initialCode: "import numpy as np\n\nX = np.ones((3, 2))\nbias = np.array([14, 15])\n\n# Примените транслирование (broadcasting)\nZ = \n",
        testCode: "import unittest\nimport numpy as np\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_broadcasting(self):\n        expected = np.array([[11., 21.], [11., 21.], [11., 21.]])\n        np.testing.assert_array_equal(student_code.Z, expected, err_msg=\"Транслирование сработало неверно.\")"
      },
      {
        id: "step-1-7",
        type: "jupyter",
        theory: "В процессе машинного обучения мы постоянно стремимся получить сводную статистику по всему массиву информации, тем самым сокращая его размерность. Этот математический процесс называется **редукцией** тензора.\n\n**Редукция** (reduction) — это операция агрегирования множества элементов тензора с целью получения более компактного представления информации. Самые распространенные операции редукции включают вычисление математической суммы, среднего арифметического (mean), а также поиск минимального или максимального значения среди элементов.\n\nОсобенность редукции в том, что ее можно применять как ко всему тензору целиком (тогда огромный многомерный массив сплющивается в один единственный скаляр), так и направленно вдоль строго определенной оси (параметр `axis`). Например, в стандартной матрице «объекты-признаки» вычисление среднего вдоль оси строк (`axis=0`) позволит нам получить вектор средних значений каждого отдельного признака для всей выборки. Если же мы выполним редукцию вдоль оси столбцов (`axis=1`), мы агрегируем всю информацию о признаках для каждого конкретного объекта (строки) по отдельности.",
        task: "Дана матрица `X`. Вычислите общую сумму всех ее элементов в переменную `sum_all`. Затем вычислите сумму значений **по каждому столбцу** (используйте `axis=0`) и сохраните результат в вектор `sum_cols`.",
        hint: "Используйте метод `X.sum()` для общей суммы и `X.sum(axis=0)` для суммы по столбцам.",
        initialCode: "import numpy as np\n\nX = np.array([\n    [7, 8],\n    [1, 9],\n    [10, 11]\n])\n\n# Выполните операции редукции\nsum_all = \nsum_cols = \n",
        testCode: "import unittest\nimport numpy as np\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_reduction(self):\n        self.assertEqual(student_code.sum_all, 21, msg=\"Общая сумма вычислена неверно.\")\n        np.testing.assert_array_equal(student_code.sum_cols, np.array([2, 16]), err_msg=\"Сумма по столбцам вычислена неверно.\")"
      },
      {
        id: "step-1-8",
        type: "jupyter",
        theory: "Для правильной передачи данных в алгоритмы машинного обучения нам часто требуется изменить их геометрическую структуру без изменения самих числовых значений в оперативной памяти. Эта процедура называется **изменением формы** (reshaping).\n\n**Изменение формы** — это процесс перераспределения элементов существующего тензора по совершенно новым осям координат при строгом сохранении их общего количества. Например, квадратная картинка размером $28 \\times 28$ пикселей может быть «сплющена» (flatten) в плоский одномерный вектор длиной $784$ элемента, чтобы ее мог принять на вход классический полносвязный слой нейросети.\n\nВсе эти структурные преобразования в конечном счете служат главной цели — подготовке данных для **векторизации**.\n\n**Векторизация** вычислений — это парадигма программирования, при которой явные, медленные циклы (например, цикл `for` в Python) полностью заменяются на высокоуровневые массивовые операции. Под капотом такие библиотеки, как NumPy, делегируют выполнение векторизованных операций глубоко оптимизированному коду на языке C, который использует векторные инструкции процессора (SIMD — Single Instruction, Multiple Data). Это позволяет процессору выполнять одну математическую команду сразу над целым блоком чисел за один такт, что ускоряет алгоритмы обучения в десятки и сотни раз.",
        task: "Сначала измените форму плоского вектора `arr` на матрицу `2x3`, сохранив ее в `arr_reshaped`. Затем напишите векторизованный код для вычисления скалярного произведения двух огромных векторов `a` и `b`, чтобы сравнить его скорость с медленным циклом `for`.",
        hint: "Используйте `arr.reshape(2, 3)` для изменения формы. Для векторизованного скалярного произведения используйте функцию `np.dot(a, b)`.",
        initialCode: "import numpy as np\nimport time\n\narr = np.array([1, 7-11])\n\n# 1. Измените форму на матрицу 2x3\narr_reshaped = \n\nnp.random.seed(42)\na = np.random.rand(100000)\nb = np.random.rand(100000)\n\n# Медленный цикл (не меняйте)\nstart = time.time()\nslow_dot = 0.0\nfor i in range(len(a)):\n    slow_dot += a[i] * b[i]\nslow_time = time.time() - start\n\n# 2. Напишите векторизованное скалярное произведение\nstart = time.time()\nfast_dot = \nfast_time = time.time() - start\n\nprint(f\"Время цикла: {slow_time:.5f} сек, Векторизация: {fast_time:.5f} сек\")\n",
        testCode: "import unittest\nimport numpy as np\nimport student_code\n\nclass TestUserSolution(unittest.TestCase):\n    def test_reshape(self):\n        self.assertEqual(student_code.arr_reshaped.shape, (2, 3), msg=\"Форма массива arr_reshaped неверна.\")\n        np.testing.assert_array_equal(student_code.arr_reshaped, np.array([[1, 7, 8], [9-11]]), err_msg=\"Данные массива искажены после изменения формы.\")\n    \n    def test_vectorization(self):\n        expected_dot = np.dot(student_code.a, student_code.b)\n        self.assertAlmostEqual(student_code.fast_dot, expected_dot, places=5, msg=\"Векторизованное скалярное произведение вычислено неверно.\")"
      }
    ]
  }
]